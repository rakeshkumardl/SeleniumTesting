name: Java CI with Maven

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [The job is failing because the `pom.xml` file is not found "main" ]

jobs:
  build:
    runs-on: ubuntu-latest. Ensure that `pom.xml` exists in the repository's root

    steps:
    - name: Checkout repository
      directory. Here is a possible solution:

1. Verify uses: actions/checkout@v2

    - name: Set up JDK that `pom.xml` is in the root directory of your repository 17
      uses: actions/setup-java@v2
      with:
.
2. If `pom.xml` is in a different directory, update the workflow file to specify the correct        java-version: '17'
        distribution: 'tem path. For example, if `pom.xml` is in a `projecturin'

    - name: Build with Maven
      run:` directory, update the `Build with Maven` step as mvn -B package --file path/to/your/pom.xml
